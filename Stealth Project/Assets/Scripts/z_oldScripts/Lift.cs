using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Lift : MonoBehaviour
{
    //public Transform outer_left;
    //public Transform inner_left;
    //public Transform outer_right;
    //public Transform inner_right;
    //private float inner_door_moveSpeed = 5;

    //void Update()
    //{
    //    float inner_leftX = Mathf.Lerp(inner_left.position.x, outer_left.position.x, Time.deltaTime * inner_door_moveSpeed);
    //    inner_left.position = new Vector3(inner_leftX, inner_left.position.y, inner_left.position.z);

    //    float inner_rightX = Mathf.Lerp(inner_right.position.x, outer_right.position.x, Time.deltaTime * inner_door_moveSpeed);
    //    inner_right.position = new Vector3(inner_rightX, inner_right.position.y, inner_right.position.z);
    //}
}
